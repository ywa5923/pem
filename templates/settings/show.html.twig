{% extends 'base.html.twig' %}

{% block title %}Settings{% endblock %}

{% block pageTitle %}
<a href="{{ path('settings_index') }}">Back to options list</a> |
<a href="{{ path('settings_edit', {'id': setting.id}) }}">edit</a>
{% endblock %}

{% block body %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ setting.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ setting.name }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ setting.value }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('settings/_delete_form.html.twig') }}
{% endblock %}
